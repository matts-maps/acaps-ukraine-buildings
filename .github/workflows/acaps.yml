name: ACAPS Ukraine Buildings

on:
  schedule:
    - cron: "0 3 * * 1"   # Mondays at 03:00 UTC
  workflow_dispatch:

jobs:
  fetch-acaps:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install pandas requests

      - name: Run ACAPS fetch + summarise
        env:
          ACAPS_USERNAME: ${{ secrets.ACAPS_USERNAME }}
          ACAPS_PASSWORD: ${{ secrets.ACAPS_PASSWORD }}
        run: python scripts/fetch_acaps_ukraine.py

      - name: Commit updated CSVs
        run: |
          if git diff --quiet data/; then
            echo "No changes — skipping commit"
            exit 0
          fi
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add data/*.csv
          git commit -m "Update ACAPS May–June 2026 data"
          git push
